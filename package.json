{
  "name": "orbit-ledger",
  "productName": "Orbit 星账",
  "private": true,
  "version": "1.0.1",
  "description": "Orbit 星账 · 本地优先的图谱化记账软件（Electron 桌面端 + 浏览器直连 + MCP 同进程）",
  "type": "module",
  "main": "electron/main.js",
  "scripts": {
    "start": "electron .",
    "make:icon": "electron electron/scripts/make-icon.js",
    "shots": "electron electron/scripts/make-shots.js",
    "build:web": "node webapp/build.mjs",
    "verify:web": "electron electron/scripts/verify-webapp.js",
    "pack": "electron-builder --win --dir --publish never",
    "dist": "electron-builder --win nsis --publish never"
  },
  "devDependencies": {
    "electron": "^44.3.0",
    "electron-builder": "^26.15.3",
    "esbuild": "^0.28.2"
  },
  "build": {
    "appId": "com.fogjw.orbit-ledger",
    "productName": "Orbit 星账",
    "directories": {
      "buildResources": "assets",
      "output": "release"
    },
    "files": [
      "assets/**/*",
      "core/**/*",
      "electron/**/*",
      "web/**/*",
      "server/src/**/*",
      "server/package.json",
      "server/node_modules/**/*",
      "!server/test/**/*",
      "!server/scripts/**/*",
      "!server/data/**/*",
      "!**/*.map"
    ],
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        }
      ],
      "icon": "assets/icon.png"
    },
    "nsis": {
      "include": "electron/installer.nsh",
      "oneClick": false,
      "perMachine": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "Orbit 星账",
      "artifactName": "Orbit-Setup-${version}.exe"
    }
  },
  "dependencies": {
    "sql.js": "^1.14.2"
  }
}
